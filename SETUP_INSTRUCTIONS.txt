# BaseMatch - Web3 Dating App Setup Guide

## 🎯 Project Overview

BaseMatch is a Web3 dating application built on Base blockchain with Farcaster Mini App integration.

### Features:
- ✅ Soulbound Profile NFTs (non-transferable)
- ✅ Decentralized matching system
- ✅ Optional USDC staking for dates
- ✅ On-chain reputation & ratings
- ✅ Achievement NFT badges
- ✅ Farcaster Mini App compatible

## 📋 Prerequisites

- Node.js 18+ and npm
- A Web3 wallet (MetaMask, Coinbase Wallet, etc.)
- WalletConnect Project ID (get from https://cloud.walletconnect.com/)
- Base Account (for Farcaster Mini App deployment)

## 🚀 Installation Steps

### 1. Install Dependencies

```bash
cd frontend
npm install
```

### 2. Configure Environment

Create `frontend/.env.local`:

```env
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_ENABLE_TESTNETS=true
```

### 3. Update Contract Addresses

After deploying smart contracts, update `frontend/lib/contracts.ts` with your deployed contract addresses.

### 4. Configure Farcaster Mini App

1. Update `frontend/public/.well-known/farcaster.json`:
   - Replace `ownerAddress` with your Base Account address
   - Replace all `https://your-domain.com` with your actual domain
   
2. Generate Account Association:
   - Deploy your app to a public URL (Vercel, Netlify, etc.)
   - Visit https://base.build (Base Build tools)
   - Use the Account Association tool to generate credentials
   - Copy the generated fields into `accountAssociation` in farcaster.json

3. Update Meta Tags in `frontend/app/layout.tsx`:
   - Replace image URLs with your actual hosted images
   - Ensure images meet Farcaster specs:
     * Embed image: 3:2 aspect ratio, min 600x400px
     * Splash image: 200x200px
     * Icon: PNG format recommended

### 5. Run Development Server

```bash
cd frontend
npm run dev
```

Visit http://localhost:3000

## 📦 Smart Contract Deployment

### Prerequisites
- Hardhat or Foundry
- Base Sepolia ETH for testnet deployment
- Base ETH for mainnet deployment

### Deployment Order

1. Deploy ProfileNFT contract
2. Deploy Matching contract (pass ProfileNFT address)
3. Deploy Staking contract (pass USDC and Matching addresses)
4. Deploy Reputation contract (pass Matching address)
5. Deploy Achievement contract

### Base Mainnet USDC Address
```
0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913
```

### Example Deployment Script (Hardhat)

```javascript
const { ethers } = require("hardhat");

async function main() {
  // Deploy ProfileNFT
  const ProfileNFT = await ethers.getContractFactory("ProfileNFT");
  const profileNFT = await ProfileNFT.deploy();
  await profileNFT.deployed();
  console.log("ProfileNFT deployed to:", profileNFT.address);

  // Deploy Matching
  const Matching = await ethers.getContractFactory("Matching");
  const matching = await Matching.deploy(profileNFT.address);
  await matching.deployed();
  console.log("Matching deployed to:", matching.address);

  // Deploy Staking
  const USDC_ADDRESS = "0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913";
  const Staking = await ethers.getContractFactory("Staking");
  const staking = await Staking.deploy(USDC_ADDRESS, matching.address);
  await staking.deployed();
  console.log("Staking deployed to:", staking.address);

  // Deploy Reputation
  const Reputation = await ethers.getContractFactory("Reputation");
  const reputation = await Reputation.deploy(matching.address);
  await reputation.deployed();
  console.log("Reputation deployed to:", reputation.address);

  // Deploy Achievement
  const Achievement = await ethers.getContractFactory("Achievement");
  const achievement = await Achievement.deploy();
  await achievement.deployed();
  console.log("Achievement deployed to:", achievement.address);
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
```

## 🎨 Customization

### Update Branding
- Colors defined in component styles (pink/purple gradient theme)
- Font: Lexend (configured in layout.tsx)
- Logo: Update emoji or add custom logo in header

### Mini App Dimensions
- Mobile: Adapts to device
- Web: Fixed at 424x695 pixels (Farcaster spec)

## 🌐 Deployment

### Frontend Deployment (Vercel/Netlify)

1. Push code to GitHub
2. Connect repository to Vercel/Netlify
3. Set environment variables
4. Deploy!

### Post-Deployment Steps

1. Update farcaster.json with production URLs
2. Generate Account Association credentials
3. Test Mini App with Base Preview tool: https://base.build/preview
4. Publish to Base app by creating a post with your app URL

## 🔧 Troubleshooting

### Common Issues

**1. "Module not found" errors**
- Run `npm install` in the frontend directory
- Check that all imports use correct paths

**2. Wallet connection fails**
- Verify WALLETCONNECT_PROJECT_ID is set correctly
- Ensure you're on Base network in your wallet

**3. Contract interactions fail**
- Verify contract addresses in lib/contracts.ts
- Check you have Base ETH for gas fees
- Ensure contracts are deployed and verified

**4. Mini App doesn't load**
- Check farcaster.json is accessible at /.well-known/farcaster.json
- Verify Account Association is correct
- Test with Base Preview tool first

## 📚 Resources

- Base Docs: https://docs.base.org
- Farcaster Mini Apps: https://miniapps.farcaster.xyz
- RainbowKit Docs: https://rainbowkit.com
- Wagmi Docs: https://wagmi.sh

## 🛠 Tech Stack

- **Frontend**: Next.js 14, React 19, TypeScript
- **Styling**: Tailwind CSS
- **Web3**: Wagmi, Viem, RainbowKit
- **Blockchain**: Base (Ethereum L2)
- **Smart Contracts**: Solidity 0.8.20
- **Mini App SDK**: Farcaster Mini App SDK

## 📄 License

MIT License - Feel free to use and modify!

## 🤝 Contributing

Contributions welcome! Please open an issue or PR.

## ⚠️ Important Notes

1. **Test thoroughly** on Base Sepolia testnet before mainnet
2. **Smart contracts are not audited** - use at your own risk
3. **IPFS recommended** for profile photos (use Pinata, NFT.Storage, etc.)
4. **Reputation is public** - all ratings and stats are on-chain
5. **Soulbound NFTs** cannot be transferred - one profile per address

## 🎯 Next Steps

1. Install dependencies: `cd frontend && npm install`
2. Get WalletConnect Project ID
3. Deploy smart contracts to Base Sepolia
4. Update contract addresses in code
5. Test locally
6. Deploy to production
7. Set up Farcaster Mini App integration
8. Launch! 🚀

Good luck building the future of Web3 dating! 💖
